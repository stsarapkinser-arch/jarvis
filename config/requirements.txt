ollama>=0.3
# httpx: async-клиент к нативному llama-server (OpenAI REST). Главный мозг
# теперь ОТДЕЛЬНЫЙ процесс (llama-server из llama.cpp, Vulkan) — собирается и
# запускается через ./setup_server.sh + jarvis-llm.service. Python весов НЕ грузит.
httpx>=0.27
# llama-cpp-python больше НЕ нужен в рантайме (in-process инференс убран).
# Оставлен закомментированным как опциональный fallback для оффлайн-экспериментов.
# llama-cpp-python>=0.3.0
chromadb>=0.5
vosk>=0.3.45
sounddevice>=0.4
PyQt6>=6.6
qasync>=0.27
psutil>=5.9
dbus-next>=0.2.3
# Aegis HUD: FFT analysis of the Piper PCM stream → core sphere pulse.
numpy>=1.26

# Optional system tools that subsystems will use when present (graceful
# fallback if missing):
#   bubblewrap (apt: bubblewrap) → primary sandbox for shadow_exec
#   podman                       → fallback container sandbox
#   qdbus / wl-paste / xclip     → clipboard read for Mnemosyne
