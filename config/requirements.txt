ollama>=0.3
# llama-cpp-python: in-process LLM с iGPU offloading на Intel N100. ставится
# через setup_igpu.sh с CMAKE_ARGS под выбранный backend (OpenCL/Vulkan/CPU).
# Версионный потолок берём только из setup-скрипта, тут — нижняя граница
# чтобы requirements.txt оставался информативным.
llama-cpp-python>=0.3.0
chromadb>=0.5
vosk>=0.3.45
sounddevice>=0.4
PyQt6>=6.6
qasync>=0.27
psutil>=5.9
dbus-next>=0.2.3
# Aegis HUD: FFT analysis of the Piper PCM stream → core sphere pulse.
numpy>=1.26

# Optional system tools that subsystems will use when present (graceful
# fallback if missing):
#   bubblewrap (apt: bubblewrap) → primary sandbox for shadow_exec
#   podman                       → fallback container sandbox
#   qdbus / wl-paste / xclip     → clipboard read for Mnemosyne
