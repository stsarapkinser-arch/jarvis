"""Golden-набор «фраза → ожидаемая категория» для оценки роутера.

Это фундамент измеримости (рычаг №7 из плана level-up): без набора истин
«стало лучше» — вера, а не инженерия. Здесь — РАЗНООБРАЗНЫЕ перефразировки
(не только якоря из router._EXEMPLARS), чтобы видеть реальную полноту
маршрутизации, а не переобучение на собственные примеры.

Метрика, которую считает harness, — recall на категорию и общая точность. При
росте каталога до сотен навыков именно этот набор покажет, где роутер начинает
ошибаться (см. обсуждение recall@k с оператором)."""
from __future__ import annotations

from src.inference.router import IntentCategory

_SYS = IntentCategory.SYSTEM_OPS
_UI = IntentCategory.UI_CONTROL
_PEN = IntentCategory.PENTEST_RECON
_CONV = IntentCategory.CONVERSATION

# (фраза, ожидаемая категория). Фразы подобраны в досягаемости лексикона;
# намеренно включены синонимы и разговорные формы.
DATASET: tuple[tuple[str, IntentCategory], ...] = (
    # ─────────────── SYSTEM_OPS ───────────────
    ("установи пакет nginx", _SYS),
    ("обнови систему через apt", _SYS),
    ("удали этот пакет", _SYS),
    ("покажи запущенные процессы", _SYS),
    ("убей зависший процесс", _SYS),
    ("сколько свободно на диске", _SYS),
    ("сколько занято памяти", _SYS),
    ("какая нагрузка на процессор", _SYS),
    ("перезапусти сервис", _SYS),
    ("останови демон", _SYS),
    ("покажи логи systemctl", _SYS),
    ("смонтируй диск", _SYS),
    ("поменяй права на файл", _SYS),
    ("сделай бэкап", _SYS),
    ("запусти скрипт обслуживания", _SYS),
    ("проверь journal на ошибки", _SYS),
    ("установи обновления pip", _SYS),
    ("покажи переменные окружения", _SYS),

    # ─────────────── UI_CONTROL ───────────────
    ("сделай ярче экран", _UI),
    ("убавь яркость", _UI),
    ("прибавь громкость", _UI),
    ("выключи звук", _UI),
    ("сверни все окна", _UI),
    ("закрой это окно", _UI),
    ("разверни окно на весь экран", _UI),
    ("переключи рабочий стол", _UI),
    ("сделай скриншот", _UI),
    ("смени обои", _UI),
    ("включи тёмную тему", _UI),
    ("открой настройки", _UI),
    ("открой меню приложений", _UI),
    ("покажи панель виджетов", _UI),
    ("что на экране", _UI),
    ("прочитай экран", _UI),
    ("какое окно сейчас активно", _UI),
    ("переключи фокус на другой монитор", _UI),

    # ─────────────── PENTEST_RECON ───────────────
    ("просканируй сеть nmap", _PEN),
    ("проверь открытые порты", _PEN),
    ("запусти скан хоста", _PEN),
    ("перехвати трафик в wireshark", _PEN),
    ("сделай дамп tcpdump", _PEN),
    ("поищи уязвимости cve", _PEN),
    ("запусти разведку по подсети", _PEN),
    ("проверь arp таблицу", _PEN),
    ("прогони nikto по сайту", _PEN),
    ("запусти masscan", _PEN),
    ("сними трафик на интерфейсе", _PEN),
    ("проведи пентест хоста", _PEN),
    ("поищи wifi сети", _PEN),
    ("анализируй pcap", _PEN),
    ("брутфорс по словарю", _PEN),
    ("снифай пакеты", _PEN),

    # ─────────────── CONVERSATION ───────────────
    ("расскажи анекдот", _CONV),
    ("как дела", _CONV),
    ("что ты думаешь о свободе воли", _CONV),
    ("кто ты такой", _CONV),
    ("привет", _CONV),
    ("спасибо", _CONV),
    ("почему небо голубое", _CONV),
    ("объясни как работает квантовый компьютер", _CONV),
    ("расскажи о себе", _CONV),
    ("какое твоё мнение", _CONV),
    ("давай поговорим о философии", _CONV),
    ("в чём смысл жизни", _CONV),
    ("как тебя зовут", _CONV),
    ("что важнее свобода или порядок", _CONV),
)

__all__ = ["DATASET"]
